# ML_HW4
1. In this task, we process a sentence using basic NLP steps.  
First, the text is split into words and common stopwords are removed.  
Then, words are converted to their base form using lemmatization.  
Finally, only important words like nouns and verbs are kept.
